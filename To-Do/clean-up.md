# Clean-up

1. List all files in the project, identify unnecessary ones, and remove them.
2. Audit `environment.yml` — determine whether Conda is still needed or if a lighter alternative like `uv` can replace it.
3. Review the existing code in `gdchol_rank.py`, `chol_rank_GD.ipynb`, and `gan_bes_search.py`. Improve clarity and efficiency where possible.