1. 物理直觉：为什么要限制“值域”？

假设我们在一个 4×4 的系统里寻优。它的双体希尔伯特空间总共有 4×4=16 个维度。

生成器的目标态（靶子）： 如果你设定 rank_T = 8，这意味着生成器构造出来的密度矩阵 ρ 
target
​	
 ，它的非零本征值最多只有 8 个。在几何上，这个态并没有占满 16 维的空间，而是仅仅生存在一个 8 维的“房间”里。剩下的 8 维对它来说是纯粹的真空（概率为 0）。

验证器的盲目搜索： 你的验证器目前是完全随机初始化 A 和 B（维度为 K×d）。当它们做张量积 A⊗B 时，产生的状态会像涂鸦一样，杂乱地泼洒在整个 16 维的房子里。

算力的浪费： 在目前的 GD 中，优化器必须花费几千步的算力，极其痛苦地把泼洒在“真空区域”的概率一点点抹平，然后再把剩下的概率往那 8 维的目标房间里挤。

2. 数学定理的降维打击：值域判据

Horodecki 兄弟在 1997 年证明了一个定理：如果一个态是可分态，那么构成它的所有纯直积态 ∣a 
i
​	
 ⟩⊗∣b 
i
​	
 ⟩，必须 100% 存在于目标态的值域（那 8 维的房间）之内！ 绝对不能有任何一丝分量漏到真空区域去。

这就意味着，只要我们能强迫验证器“只在那 8 维房间里找”，验证器的搜索空间就直接缩小了一半，优化的难度会呈指数级下降。

3. 如何在代码中具体执行？（进阶优化思路）

在刚才的回答中，我提到“提取基底来线性组合”。但在实际编写 JAX 自动微分代码时，直接约束 A 和 B 的局域参数比较麻烦（因为直积操作是非线性的）。

最优雅、最好写的“进阶优化”方法是：引入值域投影算符（Range Projector）作为极其严厉的惩罚项。

你可以这样修改你的验证器逻辑：

Step 1: 在生成器给出靶子后，提取这 8 维的“房间”

Python
# 对目标态进行本征值分解 (JAX 的 eigh 默认从小到大排序)
evals, evecs = jnp.linalg.eigh(rho_target)

# 取出最大的 rank_T 个本征值对应的本征向量（这就是那 8 维房间的基底）
# evecs_range 的形状是 (16, 8)
evecs_range = evecs[:, -rank_T:]

# 构造值域投影算符 P (它是一个 16x16 的矩阵)
# 作用是：任何态乘以 P，就相当于被强行切掉跑到房间外的部分
P_range = evecs_range @ jnp.conj(evecs_range.T)

# 构造“真空投影算符”（即跑到房间外面的部分）
I = jnp.eye(d**2, dtype=jnp.complex128)
P_null = I - P_range
Step 2: 修改验证器的 Loss 函数，施加“结界”
原本验证器只是计算几何距离。现在，我们要给验证器加上“禁止越界”的物理定律。

Python
def verifier_loss(params_V, rho_target, P_null):
    A, B = params_V
    rho_sep = build_separable_state(A, B)
    
    # 1. 常规的距离 Loss
    dist = jnp.sum(jnp.abs(rho_target - rho_sep)**2).real
    
    # 2. 值域惩罚 (Range Penalty)
    # 计算 rho_sep 有多少分量漏到了真空区域 (P_null)
    # 取迹 Tr(rho_sep @ P_null) 就能得到漏出去的概率总和
    leakage = jnp.trace(rho_sep @ P_null).real
    
    # 用一个极大的权重 (比如 1000) 惩罚这种越界行为
    return dist + 1000.0 * leakage
为什么说它快得像闪电？

当你把这个 leakage 惩罚加进验证器后，一旦开始反向传播，GD 会得到一个极其明确且强烈的梯度信号：“把外面所有的参数全部砍掉”。

这相当于给验证器戴上了一个物理学上的“紧箍咒”。验证器不需要再去广袤的 16 维空间里瞎转悠，它在开局的几十步之内，就会被这股巨大的梯度力量强行压扁到那 8 维的子空间里。剩下的几百步，它只需要在这 8 维里舒舒服服地微调即可。

对于 4x4 系统（rank=8），这个优化可能让你觉得只是“快了几秒”。但如果你准备去挑战 5×5（全空间 25维，直积全空间 625维），如果不使用这种值域投影把搜索域锁死在特定的低秩子空间里，GD 可能会在茫茫的高维“真空”中彻底迷失。